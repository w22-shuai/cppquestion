---
export const prerender = false;

const runtime = Astro.locals.runtime;
const env = runtime?.env;
const timenow = Math.floor(Date.now() / 1000);

// =========================================================
// 1. 24小时 Token 轮转逻辑
// =========================================================
let timelast = await env.checkidentity.get("time");
timelast = timelast ? parseInt(timelast) : 0;
if ((timenow - timelast) > 86400) {
    const newToken = crypto.randomUUID();
    await env.checkidentity.put('time', timenow.toString());
    await env.checkidentity.put('cookie', newToken);
    return Astro.redirect("/login");
}


    let message = "";
    let status = ""; // "success" | "error"

// =========================================================
// 2. 鉴权逻辑
// =========================================================
    const userToken = Astro.cookies.get("auth_token")?.value;
    let serverToken = null;

    if (env?.checkidentity) {
        serverToken = await env.checkidentity.get("cookie");
    }

    if (!userToken || !serverToken || userToken !== serverToken) {
        Astro.cookies.delete("auth_token", { path: "/" });
        return Astro.redirect("/login"); // 确保这里重定向路径是你想要的
    }

// =========================================================
// 3. 插入逻辑 (POST) - [核心修改]
// =========================================================
    if (Astro.request.method === "POST") {
        try {
            const formData = await Astro.request.formData();
            const key = formData.get("key")?.toString().trim();

            if (!key) {
                message = "⚠️ 内容不能为空";
                status = "error";
            } else {
                // [修改说明]
                // 1. SQL 语句保持不变：INSERT INTO ... VALUES (?, 0)
                //    因为 id 是 AUTOINCREMENT，数据库会自动生成，不需要在这里写 id
                const stmt = env.cppquestion.prepare(
                    "INSERT INTO cppquestion (key, value) VALUES (?, 0)"
                );

                const info = await stmt.bind(key).run();

                if (info.success) {
                    // 2. [新增] 获取新生成的 ID
                    // D1 的 run() 返回结果包含 meta.last_row_id
                    const newId = info.meta?.last_row_id ?? "?";

                    message = `✅ 录入成功 [ID: #${newId}]`;
                    status = "success";
                } else {
                    message = "❌ 提交失败 (D1 Error)";
                    status = "error";
                }
            }
        } catch (e) {
            // 捕获唯一性约束或其他错误
            if (e.message && (e.message.includes("UNIQUE") || e.message.includes("Constraint"))) {
                message = "⚠️ 提交失败：该内容已存在库中";
            } else {
                console.error(e);
                message = "❌ 系统错误: " + e.message;
            }
            status = "error";
        }
    }

---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>数据录入终端</title>
    <style>
        /* ==================== 全局背景 (保持一致) ==================== */
        body {
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            margin: 0;
            padding: 40px 20px;
            color: #fff;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            box-sizing: border-box;
            background-image: url('/shuai.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-repeat: no-repeat;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0; left: 0; right: 0; bottom: 0;
            background: rgba(0, 0, 0, 0.75);
            z-index: -1;
            backdrop-filter: blur(3px);
        }

        /* ==================== 导航栏 ==================== */
        .nav {
            width: 100%;
            max-width: 600px;
            margin-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        /* 修改：返回链接改为指向 /cppquestion (列表页) */
        .back-link {
            color: #00f2ff;
            text-decoration: none;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 1px;
            padding: 8px 15px;
            border: 1px solid rgba(0, 242, 255, 0.3);
            border-radius: 4px;
            transition: all 0.3s;
            background: rgba(0,0,0,0.3);
        }
        .back-link:hover {
            background: rgba(0, 242, 255, 0.1);
            border-color: #00f2ff;
            box-shadow: 0 0 10px rgba(0, 242, 255, 0.4);
        }

        /* ==================== 主容器 ==================== */
        .box {
            width: 100%;
            max-width: 600px;
            padding: 40px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            backdrop-filter: blur(10px);
            box-shadow: 0 0 30px rgba(0,0,0,0.5);
            box-sizing: border-box;
        }

        h3 {
            margin-top: 0;
            text-align: center;
            font-weight: 300;
            letter-spacing: 2px;
            color: #eee;
            margin-bottom: 30px;
            text-transform: uppercase;
            border-bottom: 1px solid rgba(255,255,255,0.1);
            padding-bottom: 15px;
        }

        /* ==================== 表单元素 ==================== */
        label {
            display: block;
            margin-bottom: 10px;
            color: #aaa;
            font-size: 14px;
            letter-spacing: 1px;
        }

        textarea {
            width: 100%;
            min-height: 250px;
            padding: 15px;
            box-sizing: border-box;
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 6px;
            color: #fff;
            font-size: 16px;
            font-family: inherit;
            resize: vertical;
            outline: none;
            transition: all 0.3s;
            line-height: 1.6;
        }

        textarea:focus {
            border-color: #0aff60;
            background: rgba(0, 0, 0, 0.5);
            box-shadow: 0 0 15px rgba(10, 255, 96, 0.2);
        }

        textarea::placeholder {
            color: rgba(255, 255, 255, 0.2);
        }

        button {
            width: 100%;
            margin-top: 30px;
            padding: 15px;
            background: rgba(10, 255, 96, 0.1);
            color: #0aff60;
            border: 1px solid #0aff60;
            border-radius: 6px;
            font-size: 16px;
            font-weight: bold;
            letter-spacing: 2px;
            text-transform: uppercase;
            cursor: pointer;
            transition: all 0.3s;
            animation: pulse-green 3s infinite;
        }

        button:hover {
            background: #0aff60;
            color: #000;
            box-shadow: 0 0 20px rgba(10, 255, 96, 0.6);
        }

        @keyframes pulse-green {
            0% { box-shadow: 0 0 0 0 rgba(10, 255, 96, 0.4); }
            50% { box-shadow: 0 0 0 10px rgba(10, 255, 96, 0); }
            100% { box-shadow: 0 0 0 0 rgba(10, 255, 96, 0); }
        }

        /* ==================== 消息提示 ==================== */
        .msg {
            margin-bottom: 20px;
            padding: 15px;
            border-radius: 6px;
            width: 100%;
            max-width: 600px;
            text-align: center;
            box-sizing: border-box;
            backdrop-filter: blur(5px);
            font-weight: bold;
        }
        .msg.success {
            background: rgba(10, 255, 96, 0.15);
            border: 1px solid #0aff60;
            color: #0aff60;
            box-shadow: 0 0 20px rgba(10, 255, 96, 0.2);
        }
        .msg.error {
            background: rgba(255, 50, 50, 0.15);
            border: 1px solid #ff3232;
            color: #ff3232;
            box-shadow: 0 0 20px rgba(255, 50, 50, 0.2);
        }

        .hint {
            font-size: 12px;
            color: rgba(255, 255, 255, 0.3);
            text-align: right;
            margin-top: 5px;
            font-family: monospace;
        }
    </style>
</head>
<body>

<div class="nav">
    <!-- 修改了这里的 href，让它指回列表页 /cppquestion (如果你的列表页路径是这个的话) -->
    <a href="/cppquestion" class="back-link">
        &lt; 返回列表
    </a>
    <span style="color: rgba(255,255,255,0.3); font-size: 12px; font-family: monospace;">SYSTEM::INSERT_MODE</span>
</div>

{message && <div class={`msg ${status}`}>{message}</div>}

<div class="box">
    <h3>录入新数据</h3>

    <form method="POST">
        <label>DATA INPUT BLOCK</label>

        <textarea
                name="key"
                placeholder="在此输入问题内容... (支持换行)"
                required
                autofocus></textarea>

        <div class="hint">AUTO_INCREMENT: ON | DEFAULT_VALUE: 0</div>

        <button type="submit">执行录入</button>
    </form>
</div>

</body>
</html>